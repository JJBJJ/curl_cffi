name: Build curl-cffi for Android

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ 检出代码
        uses: actions/checkout@v4

      - name: 📥 克隆 Chaquopy
        run: |
          git clone --depth 1 https://github.com/chaquo/chaquopy.git
          echo "✅ Chaquopy 克隆完成"

      - name: 🐍 设置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 安装系统工具
        run: |
          sudo apt-get update
          sudo apt-get install -y patch patchelf wget unzip build-essential pkg-config libssl-dev \
            python3-dev python3-setuptools python3-wheel python3-pip \
            libcurl4-openssl-dev libssl-dev libxml2-dev libxslt-dev \
            python3-lxml python3-cffi
          echo "✅ 系统工具安装完成"

      - name: ⬇️ 下载 Python 目标文件
        run: |
          echo "下载 Python 3.9.20-1 目标文件..."
          mkdir -p chaquopy/maven/com/chaquo/python/target/3.9.20-1
          cd chaquopy/maven/com/chaquo/python/target/3.9.20-1

          wget https://repo1.maven.org/maven2/com/chaquo/python/target/3.9.20-1/target-3.9.20-1-arm64-v8a.zip
          wget https://repo1.maven.org/maven2/com/chaquo/python/target/3.9.20-1/target-3.9.20-1-armeabi-v7a.zip
          wget https://repo1.maven.org/maven2/com/chaquo/python/target/3.9.20-1/target-3.9.20-1-stdlib.zip
          wget https://repo1.maven.org/maven2/com/chaquo/python/target/3.9.20-1/target-3.9.20-1-stdlib-pyc.zip

          unzip -o target-3.9.20-1-arm64-v8a.zip
          unzip -o target-3.9.20-1-armeabi-v7a.zip
          unzip -o target-3.9.20-1-stdlib.zip
          unzip -o target-3.9.20-1-stdlib-pyc.zip
          echo "✅ Python 目标文件下载完成"

      - name: 🔧 设置 Android SDK
        run: |
          echo "设置 Android SDK..."
          mkdir -p android-sdk/cmdline-tools
          cd android-sdk/cmdline-tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q commandlinetools-linux-9477386_latest.zip
          mkdir -p latest
          mv cmdline-tools/* latest/ 2>/dev/null || true
          echo "ANDROID_HOME=$PWD/.." >> $GITHUB_ENV
          echo "✅ Android SDK 设置完成"

      - name: 📚 准备构建环境
        run: |
          cd chaquopy/server/pypi
          pip install -r requirements.txt
          
          # 下载 libcurl 头文件
          mkdir -p /tmp/curl-headers
          cd /tmp/curl-headers
          wget -q https://github.com/curl/curl/archive/refs/tags/curl-7_85_0.tar.gz
          tar -xzf curl-7_85_0.tar.gz
          
          # 创建正确的目录结构
          mkdir -p /tmp/curl-include/curl
          cp curl-curl-7_85_0/include/curl/*.h /tmp/curl-include/curl/ 2>/dev/null || true
          
          # 验证头文件
          echo "✅ 已安装的 curl 头文件:"
          ls -la /tmp/curl-include/curl/
          
          # 安装构建依赖
          pip install cffi>=1.0.0 pycparser
          
          echo "✅ Python 依赖安装完成"

      - name: 📝 创建 curl-cffi 配方
        run: |
          cd chaquopy/server/pypi
          mkdir -p packages/curl-cffi

          cat > packages/curl-cffi/meta.yaml <<'EOF'
          package:
            name: curl-cffi
            version: 0.13.0

          source:
            url: https://github.com/yifeikong/curl_cffi/archive/refs/tags/v0.13.0.tar.gz

          build:
            number: 0
            script: |
              export C_INCLUDE_PATH=/tmp/curl-include:$PREFIX/include/python3.9
              export CPLUS_INCLUDE_PATH=/tmp/curl-include:$PREFIX/include/python3.9
              export LDFLAGS="-L/tmp/curl-lib -L$PREFIX/lib"
              python -m pip install --no-deps -v .

          requirements:
            host:
              - python
          EOF
                echo "✅ curl-cffi 配方写入完成"

      - name: 🏗️ 构建 Android 包
        run: |
          cd chaquopy/server/pypi
          for abi in arm64-v8a armeabi-v7a; do
            echo "构建 $abi 架构..."
            ./build-wheel.py --python 3.9 --abi "$abi" packages/curl-cffi/ || echo "⚠️ $abi 架构构建失败"
          done

      - name: ⬆️ 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: curl-cffi-android-wheels
          path: chaquopy/server/pypi/dist/
