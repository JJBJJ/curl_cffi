name: Build curl-cffi for Android

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🛠️ 检出代码
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: 📥 克隆 Chaquopy
      run: |
        git clone --depth 1 https://github.com/chaquo/chaquopy.git
        echo "CHAQUOPY_DIR=$PWD/chaquopy" >> $GITHUB_ENV

    - name: 🐍 设置 Python 3.8
      uses: actions/setup-python@v4
      with:
        python-version: '3.8'

    - name: 📦 安装系统工具
      run: |
        sudo apt-get update
        sudo apt-get install -y patch patchelf wget unzip pkg-config libssl-dev

    - name: ⬇️ 下载 Python 目标文件
      run: |
        echo "下载 Python 3.8 目标文件..."
        mkdir -p chaquopy/maven/com/chaquo/python/target
        cd chaquopy/maven/com/chaquo/python/target
        wget https://repo1.maven.org/maven2/com/chaquo/python/target/3.8.5-1/target-3.8.5-1.zip
        unzip target-3.8.5-1.zip
        echo "✅ Python 目标文件下载完成"

    - name: 🔧 设置 Android SDK
      run: |
        mkdir -p android-sdk/cmdline-tools/latest
        cd android-sdk/cmdline-tools/latest
        wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
        unzip commandlinetools-linux-9477386_latest.zip
        echo "ANDROID_HOME=$PWD/../../.." >> $GITHUB_ENV

    - name: 📚 安装 Python 依赖
      run: |
        cd chaquopy/server/pypi
        pip install -r requirements.txt

    - name: 📝 创建 curl-cffi 配方
      run: |
        cd chaquopy/server/pypi
        mkdir -p packages/curl-cffi
        
        cat > packages/curl-cffi/meta.yaml << 'EOF'
        package:
          name: curl-cffi
          version: 0.7.0

        source:
          path: ../../../../

        build:
          number: 0

        requirements:
          build:
            - python
            - pip
            - setuptools
            - wheel
            - cffi
            - pycparser
          host:
            - python
            - cffi
            - pycparser
        EOF

    - name: 🏗️ 构建 Android 包
      run: |
        cd chaquopy/server/pypi
        for abi in arm64-v8a armeabi-v7a; do
          echo "构建 $abi 架构..."
          ./build-wheel.py --python 3.8 --abi $abi packages/curl-cffi/ || echo "$abi 架构构建失败"
        done

    - name: ⬆️ 上传产物
      uses: actions/upload-artifact@v4
      with:
        name: curl-cffi-android-wheels
        path: chaquopy/server/pypi/dist/
        retention-days: 30
